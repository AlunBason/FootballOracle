@model FootballOracle.Models.ViewModels.Approvable.Countries.CountryTeamsViewModel

@{ ViewBag.Title = string.Format("'{0}' teams", Model.ToString()); }

@Html.DisplayForModel()

<div class="row">
    <div class="col-md-12">

        @if (Model.InitialLetters != null)
        {
            <div class="text-left">
                <span>Select initial character: </span>
                @foreach (var initialLetter in Model.InitialLetters)
                {
                    var classValue = initialLetter != Model.SelectedInitialLetter ? "btn btn-xs btn-default" : "btn btn-xs btn-primary";

                    @Html.ActionLink(initialLetter.ToString(), "Teams", new { id = Model.ShortPrimaryKey, dt = Model.ViewDate.ToUrlString(), sl = initialLetter }, new { @class = classValue })
                }
            </div>

            <br />
        }

        <dl>
            @foreach (var item in Model.ChildTeamViewModels)
            {
                @Html.DisplayFor(m => item, "ISearchResult");
            }
        </dl>
    </div>
</div>

@Html.SelectTab("teams")

