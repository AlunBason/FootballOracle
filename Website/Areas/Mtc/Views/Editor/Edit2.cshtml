@model FootballOracle.Models.ViewModels.Approvable.Matches.MatchEditorViewModel

@{ ViewBag.Title = "Edit match (Page 2):"; }

@Html.DisplayForModel()

<div class="row">
    <div class="col-md-6 col-lg-offset-3">
        @Html.EditorForModel("Editor2")
    </div>
</div>

@Html.SelectTab("edit")